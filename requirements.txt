# Core app
streamlit>=1.32.0
python-dotenv>=1.0.1

# LLM (Groq is the only allowed API)
groq>=0.11.0

# PDF parsing + metadata
pymupdf>=1.24.0
pymupdf4llm>=0.0.17

# Local embeddings (Groq has no embeddings endpoint)
sentence-transformers>=2.7.0

# Vector store with metadata filtering
chromadb>=0.5.0

# Images
pillow>=10.0.0
